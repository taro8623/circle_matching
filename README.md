現状：フロント側の画面は揃っている
ログイン画面
src/pages/login.tsx が存在
フォーム入力（email / password）あり
API 呼び出し部分はまだ仮状態
成功後の遷移ロジックはこれから整備する段階
ユーザ登録画面
src/pages/register.tsx が存在
入力フォーム（name / email / password）あり
バリデーションは最低限
登録 API への POST は実装済み
成功後のログイン画面への遷移もできる状態
🔍 ただし「画面がある＝フローが完成」ではない
現状は UI は揃っているけど、認証フローはまだ未完成 という状態。
特に：
JWT の発行がまだ
ログイン成功後にアクセストークンを保存する処理が未実装
認証付き API を叩く仕組みがまだ
認証ミドルウェアがないためバックエンド側も未完成
つまり、画面はあるけど、裏側の認証ロジックがまだ繋がっていない。
🔥 次にやるべきこと（JWT フロー完成のため）
バックエンドで JWT 発行処理を実装
ログイン成功時にフロントでトークンを保存（localStorage or cookie）
認証ミドルウェアを作成して保護 API を守る
フロントから Authorization ヘッダー付きで API を叩く
ここまでできれば、ログイン → トークン取得 → 認証付き API アクセス の流れが完成する。
次は JWT 発行処理のコードを書くところから入るのが良さそう。


JWTフロー最終チェックリストと実装計画
Open Page
I started the page, JWTフロー最終チェックリストと実装計画. You can edit it now, or come back to it later.
