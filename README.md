## 🚀 認証機能の実装進捗

### ✅ 完了済みタスク
- **ユーザー登録（Signup）機能**
  - 登録画面（UI）実装済み
  - パスワードは bcrypt で安全にハッシュ化
  - DB へユーザー情報を保存
- **ログイン（Login）機能**
  - ログイン画面（UI）実装済み
  - パスワード検証（bcrypt）
  - JWT アクセストークン発行（HS256 / exp 付き）
- **JWT 認証フローの基礎**
  - `create_access_token` によるトークン生成
  - `get_current_user` によるトークン検証
  - `/me` API を認証必須エンドポイントとして実装

---

### 🔧 進行中 / 改善予定のタスク
- **SECRET_KEY を .env に移動（セキュリティ強化）**
- **Signup 時の email 重複チェック追加**
- **エラーレスポンスの統一（401 / 404 / 400）**
- **認証ロジックのファイル分割（routers / services / schemas）**
- **フロント側でのトークン保存（localStorage or Cookie）**
- **認証付き API のフロント実装**

---

### 📝 今後の予定
- **リフレッシュトークン導入の検討**
  - 保存場所（DB / Redis / Cookie）
  - ローテーション方式の採用可否
- **チーム開発向けのコード構造整備**
  - controllers / services / repositories の分離
  - エラーハンドリングの共通化
  - env スキーマ化（pydantic Settings など）
- **テスト環境の整備**
  - ユニットテスト
  - E2E テスト

---

### 📌 現状まとめ
UI（ログイン・登録）とバックエンドの JWT 認証フローは完成。  
今後は **セキュリティ強化・構造化・リフレッシュトークン導入** に進む段階。
