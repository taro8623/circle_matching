# Private Project Memory

## ローカル環境
- **Base Directory**: `/Users/fukutomitaro/dev/backend`
- **Python venv**: `./venv/bin/python`
- **Main App**: `main:app` (Port 8001)

## 個別設定
- `apply.sh` の実行には対話的な "yes" の入力が必要。
- PostgreSQL が `localhost` で稼働している必要がある。
