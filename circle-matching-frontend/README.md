タスクの状況

circle-matching-frontend フォルダに
React プロジェクトが生成された
npm install も完了している
npm run dev が自動で実行されている
http://localhost:5173/でページが表示されるのを確認
React プロジェクトが作成されているのを確認 GITの運用
* React プロジェクトを Git 管理にした
* GitHub にリポジトリを作成し、コードをアップロードした
* 別PCから clone して開発を再開できる状態になった
* 今後の開発フロー（add → commit → push）が整った
* READMEの入力
REACTのインストール
サークル登録画面のUI完成
/signup 画面を作ること（サークル登録画面のUIをコピーして作れる
